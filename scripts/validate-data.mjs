import fs from 'node:fs';const data=JSON.parse(fs.readFileSync(new URL('../src/data/theories.json',import.meta.url),'utf8'));const errors=[];const sourceIds=new Set(data.sources.map(x=>x.id));const theoryIds=new Set(data.theories.map(x=>x.id));if(theoryIds.size!==data.theories.length)errors.push('duplicate theory id');for(const x of data.theories){if(!x.slug||!x.kind||!x.currentEvidence||!x.critiques)errors.push(`${x.id} incomplete`);for(const id of x.sourceIds)if(!sourceIds.has(id))errors.push(`${x.id} missing source ${id}`)}for(const x of data.relationships)if(!theoryIds.has(x.fromId)||!theoryIds.has(x.toId))errors.push(`${x.id} invalid relation`);if(data.theories.length<40)errors.push('public snapshot below 40 records');if(data.sources.length<80)errors.push('cited source set unexpectedly low');console.log(JSON.stringify({records:data.theories.length,sources:data.sources.length,relationships:data.relationships.length,errors},null,2));if(errors.length)process.exit(1);
